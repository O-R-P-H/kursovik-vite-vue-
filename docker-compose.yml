version: '3.8'
services:
  main:
    image: harbor.tsukawa.ru/backseezer/backend:latest
    container_name: main
    ports:
      - "3000:3000"
    restart: unless-stopped
    environment:
      - NODE_ENV=production
    # Убраны все volumes, которые могли перезаписывать /app
